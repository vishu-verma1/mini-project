<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-between mb-4">
            <a class="bg-blue-400 rounded-md px-3 py-2 mb-5 inline-block text-sm" href="/profile/upload">change profile</a>
            <a class="bg-red-500 rounded-md px-3 py-2 mb-5 inline-block text-sm" href="/logout">logout</a>
        </div>
        <div class=" flex item-start gap-3">
            <div class="w-10 h-10 overflow-hidden  rounded-md">
                <img class="w-full h-full object-cover " src="/images/uploads/<%= user.profilePic %>" alt="">
            </div>
        <h3 class="text-3xl ">
            <span class="font-light">Hello </span>
            <%= user.name %>😊👋🏻
        </h3>
        </div>
        <h5 class="mb-5 mt-3">create a new Post</h5>
        <form action="/post" method="post">
            <textarea name="content" placeholder="what's on your mind "
                class="p-2 block outline-none rounded-md w-1/3 resize-none bg-transparent border-zinc-800 border-2"
                id=""></textarea>
            <input class="bg-zinc-800 px-3 py-2 mt-2 rounded-md block" type="submit" value="create new post" />
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 rounded-md p-4 border-[1px] mb-4 border-zinc-700 bg-zinc-800">
                        <h4 class="text-blue-400 mb-2">@<%= user.username %>
                        </h4>
                        <p class="tracking-tight text-sm">
                            <%= post.content %>
                        </p>

                        <small class="mt-2 inline-block"><%= post.likes.length %>❤️</small>

                        <div class="btns flex gap-5 mt-1">
                            <a class="text-red-400" href="/like/<%= post.id %>">
                              <%= post.likes.indexOf(user._id) === -1 ? "Like":"Unlike" %>  </a>
                            <a class="text-zinc-400" href="/edit/<%= post._id %>">Edit</a>
                        </div>
                    </div>
                    <%})%>
            </div>


        </div>
    </div>
    </div>
</body>

</html>